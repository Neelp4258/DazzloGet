services:
  - type: web
    name: video-downloader
    env: python
    buildCommand: |
      chmod +x start.sh
      pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.9 